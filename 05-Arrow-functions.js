// const cuadrado = function (number) {
//   return number * number;
// };

// console.log(cuadrado(5));

// Arrow function

// const cuadrado = (number) => { return number * number;};

// const cuadrado = number => number * number;

const multiplicacion = (x, y) => x * y;

console.log(cuadrado(5));
console.log(multiplicacion(5, 6));
